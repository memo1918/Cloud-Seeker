// root path for all routes
// this is used by the framework to determine the root path for all routes
// each route is a unique file and thus has a unique path to it with the routes folder as root
export const ROUTE_ROOT_PATH = __dirname;
