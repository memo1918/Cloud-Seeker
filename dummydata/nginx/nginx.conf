events { worker_connections 1024; }

http {

    server {
        listen 80;

        server_name _;

        root /www/data;

        location / {
            sendfile           on;
            sendfile_max_chunk 1m;
        }

    }
}